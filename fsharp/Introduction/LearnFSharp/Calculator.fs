namespace Learnfsharp.Calculator

module Adder =
    let add x y =
        x + y
    let add' x y =
        let result = add x y
        result

module Multiplier =
    let square x = x * x
    let mult x y = x * y

